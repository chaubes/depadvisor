[project]
name = "example-project"
version = "1.0.0"
dependencies = [
    "flask==3.0.0",
    "sqlalchemy>=2.0",
    "pydantic>=2.0,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]